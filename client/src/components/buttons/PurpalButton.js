import React from "react";
import "./Buttons.css";

const PurpalButton = ({ label }) => {
  return (
    <>
      <button className="purpal-btn">{label}</button>
    </>
  );
};

export default PurpalButton;
